- event = post.postable
%h2= link_to(event.name, event_path(event))
.event_content
  = render('content_markdown', model: event)
%ul.unstyled.users
  - event.attend_users.each do|user|
    %li= image_tag(user.image, class: 'face')

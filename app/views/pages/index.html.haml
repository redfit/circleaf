.hero-unit
  %h1 circleafe
  小さなサークルの簡単コミュニケーションツール！

.row-fluid
  .span4
    %h2 ログインする
    %p サークルを作成したり、参加することができます。
    - if user_signed_in?
      = link_to('twitterでログインする', '#', class: 'btn btn-block disabled')
    - else
      = link_to('twitterでログインする', user_omniauth_authorize_path(:twitter), class: 'btn btn-info btn-block')
  .span4
    %h2 サークルを作成する
    %p 必要なのはtwitterのアカウントだけ
    - if user_signed_in?
      = link_to('サークルを作成する' , new_group_path, class: 'btn btn-warning btn-block')
    - else
      = link_to('サークルを作成する' , '#', class: 'btn btn-block disabled')

  .span4
    %h2 サークルを探す
    %p 公開サークルを探して加入することも出来ます。
    = link_to('サークルを探す', groups_path, class: 'btn btn-success btn-block')


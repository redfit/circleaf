= simple_form_for([commentable, comment], html: {class: 'form-inline'}) do |f|
  = f.input(:content, label: false, input_html: {class: 'span12 auto_clear'}, autocomplete: 'off')
  = f.submit(t('nav.common.post'), class: 'btn')
